[project]
name = "ds-acled-trends"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cryptography",
    "ocha-stratus",
    "pandas",
    "requests",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
